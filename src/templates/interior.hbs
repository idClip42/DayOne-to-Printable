{{! 
Prettier formatting gets rid of this, so we have to add it elsewhere.
<!DOCTYPE html> 
}}
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <title>Journal Export</title>

        <style>
            {{style}}
        </style>

        <!-- Bringing in "pagedjs" package -->
        <script src="../node_modules/pagedjs/dist/paged.polyfill.js"></script>
    </head>
    <body>
        <div class="page-layout" id="first-page">
            <div style="display: none">
                {{#each colorTestDates}}
                    <div style="background-color: hsl({{hue}}, 70%, 75%)">
                        {{this.date}}
                    </div>
                {{/each}}
            </div>

            <div id="stats-index" class="stats-group">
                <h2>
                    Stats
                </h2>
                <table>
                    {{#each stats}}
                        <tr>
                            <td class="label">
                                {{this.label}}
                            </td>
                            <td class="value">
                                {{this.value}}
                            </td>
                        </tr>
                    {{/each}}
                </table>
            </div>

            <div id="tag-index" class="stats-group">
                <h2>
                    Tags
                </h2>
                <table>
                    {{#each tagStats}}
                        <tr>
                            <td class="label">
                                {{this.label}}
                            </td>
                            <td class="value">
                                {{this.value}}
                            </td>
                        </tr>
                    {{/each}}
                </table>
            </div>
        </div>

        <div class="page-layout" id="entries">
            {{#each entriesHtml}}
                {{{this}}}
            {{/each}}
        </div>
    </body>
</html>