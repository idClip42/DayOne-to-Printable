<article class="entry">
    <div
        class="metadata"
        style="background: linear-gradient(to bottom, {{monthColor}}, #ffffff)"
    >
        <p class="pre-date-time">
            <span class="weekday">{{weekday}}</span>
            <span class="weather">{{weather}}</span>
        </p>

        <h2 class="date">
            {{dateTime}}
        </h2>

        <div class="hidden-date">
            {{pageHeaderFullDate}}
        </div>

        <p class="post-date-time">
            <span>{{location}}</span>
        </p>

        <p class="tags">
            Tags:
            {{#each tagHtmls}}
                {{this}}
            {{/each}}
        </p>
    </div>
    <div>
        {{{contentHtml}}}
    </div>
</article>